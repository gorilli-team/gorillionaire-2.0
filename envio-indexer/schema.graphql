type TokenHourlySnapshot {
  id: ID!
  lastUpdatedAt: Int!
  tokenAddress: String!
  transferCount: Int!
}

type TokenAggregateSwaps {
  id: ID!
  tokenAddress: String!
  avgAmount: BigInt!
  transferCount: Int!
  lastUpdatedAt: Int!
}
